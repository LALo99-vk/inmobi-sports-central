> [!IMPORTANT]
> Avoid rewriting published git history — force pushing, or rebasing/amending/squashing
> commits that are already pushed. This repository syncs with an external editor, and
> rewriting history there will likely lose the project history.
>
> Commits pushed to the connected branch sync back to that editor, so keep the branch
> in a working state.
